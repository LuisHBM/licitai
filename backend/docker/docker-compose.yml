services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: licitai_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: licitai
      POSTGRES_DB: licitai
    ports:
      - "5438:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-test-db.sql:/docker-entrypoint-initdb.d/init-test-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: licitai_backend
    ports:
      - "8000:8000"
    volumes:
      - ../settings.local.toml:/app/settings.local.toml:ro
      - ../.secrets.local.toml:/app/.secrets.local.toml:ro
    environment:

  tests:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        DEV: "true"
    container_name: licitai_tests
    environment:
      DATABASE_URL: postgresql://postgres:licitai@postgres:5432/licitai_test
    depends_on:
      postgres:
        condition: service_healthy
    command: python -m pytest tests/test_integration.py -v
    profiles:
      - test

volumes:
  pgdata:

networks:
  default:
    enable_ipv6: true
