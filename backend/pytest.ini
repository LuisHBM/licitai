[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: testes unitários sem dependências externas (rápido, roda em todo commit)
    integration: testes que dependem de banco de dados (roda no pipeline completo)
